name: Runtime Extension Tests

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runtime-tests:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24.16.0'
      - name: Enable Corepack
        run: corepack enable
      - name: Verify Android emulator
        run: |
          adb devices
          device_count="$(adb devices | awk 'NR > 1 && $2 == "device" { count++ } END { print count + 0 }')"
          test "$device_count" -eq 1
          boot_completed="$(adb shell getprop sys.boot_completed | tr -d '\r')"
          test "$boot_completed" = "1"
      - name: Bootstrap Brave and Chromium
        run: bash ./scripts/bootstrap.sh "$RUNNER_TEMP/brave-ext-work"
      - name: Verify overlay patches
        run: bash ./scripts/verify-patches.sh "$RUNNER_TEMP/brave-ext-work/src"
      - name: Apply overlay patches
        run: bash ./scripts/apply-patches.sh "$RUNNER_TEMP/brave-ext-work/src"
      - name: Run extension runtime gate
        run: bash ./scripts/test-extension-runtime.sh "$RUNNER_TEMP/brave-ext-work"
